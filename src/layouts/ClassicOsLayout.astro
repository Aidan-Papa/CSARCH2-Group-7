---
// This is the frontmatter. JavaScript and build logic go here.
const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title}</title>
</head>
<body>
    <slot />
</body>
</html>

<style is:global>
    /* Adding 'is:global' ensures these styles apply to the whole document, just like your old CSS file */
    @import url('https://fonts.googleapis.com/css2?family=B612:ital,wght@0,400;0,700;1,400;1,700&family=Barlow:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto+Mono:ital,wght@0,100..700;1,100..700&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap');

    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: "Roboto Mono", monospace;
    }
    html, body {
        width: 100%;
        height: 100%;
        overflow: hidden;
        background-color: #000;
    }

    .main-container {
        width: 100vw;
        height: 100vh;
        background-color: var(--main-bg-color, #a9a9a9);
        background-image: var(--main-bg-image, radial-gradient(#333 25%, transparent 25%), radial-gradient(#333 25%, transparent 25%));
        background-size: 4px 4px;
        background-position: 0 0, 2px 2px;
        border: 4px solid #000;
        display: flex;
        flex-direction: column;
        border-radius: 25px;
        overflow: hidden;
    }

    .header {
        background-color: var(--header-bg, #fff);
        height: 75px;
        border-bottom-width: 2px;
        border-bottom-color: #000;
        border-bottom-style: var(--header-border-style, solid);
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0 12px;
        box-shadow: 0px 3px 0px rgba(0, 0, 0, 0.3);
    }

    .header-left {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .header-left span{
        padding-left: 7px;
        font-size: 28px;
        font-weight: bold;
    }

    .apple-logo-img {
        height: 30px;
        width: auto;
        display: inline-block;
        vertical-align: middle;
    }

    .header-right {
        text-align: right;
        font-size: 15px;
        line-height: 1.2;
    }

    .workspace {
        display: flex;
        flex: 1;
        padding: 20px;
        padding-left: 150px;
        gap: 25px;
        height: calc(100vh - 38px);
    }

    .sidebar {
        display: flex;
        flex-direction: column;
        width: 80px;
        align-items: center;
        justify-content: space-around;
        height: 100%;
    }

    a {
        color: black;
        text-decoration: none;
        font-weight: bold;
    }

    .sidebar button {
        background: none;
        border: none;
        color: black;
        font-family: inherit;
        font-weight: bold;
        font-size: 14px;
        cursor: pointer;
        padding: 6px 10px;
    }

    .sidebar button.active {
        text-decoration: underline;
    }

    .content-window {
        flex: none !important;
        width: 60vw;
        height: 75vh;
        margin: auto;
        background-color: #fff;
        border: 2px solid #000;
        display: flex;
        flex-direction: column;
        box-shadow: 4px 4px 0px rgba(0,0,0,0.3);
    }

    .window-header {
        border-bottom: 2px solid #000;
        text-align: center;
        padding: 10px;
        font-weight: bold;
        font-size: 25px;
        letter-spacing: 1px;
        background-color: #fff;
    }

    .window-body {
        flex: 1;
        padding: 40px;
        text-align: justify;
        font-size: 16px;
        color: #000;
        overflow-y: auto; 
    }

    .body-img {
        display: block;        
        margin: 20px auto;
        width: 180px;          
        height: auto;      
        border: 1px solid #000;
    }

    .window-body h5{
        padding-top: -10px;    text-align: center;
        font-size: 13px;
        font-style: italic;
        font-weight: normal;
    }

    .window-body .subheading {
        font-weight: bold;
        margin-top: 20px;
        margin-bottom: 4px;
    }

    .window-body .placeholder-note {
        font-style: italic;
        color: #666;
        border: 1px dashed #999;
        padding: 10px;
        margin-bottom: 12px;
        background: #f5f5f5;
    }
</style>